import React, { useContext, useState} from 'react';
import ResContext from '../../context/ResContext';

export default function Diagrams( apiRes ){

    console.log(apiRes);

    return(

        <div>
            <h1>Diagram</h1>
        </div>
    )
};